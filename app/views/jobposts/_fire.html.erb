<%= form_for @job.winationships.find_by_worker_id(bidder.id),
        :html => { :method => :delete } do |f| %>
	<div class="actions">
        <%= f.hidden_field :worker_id %>
        <%= f.hidden_field :job_id %>
        <%= f.submit "Fire" %>
        </div>
<% end %>

        
<%#= form_for(current_user.relationships.build(:followed_id => @user.id),
     :remote => true) do |f| %>
