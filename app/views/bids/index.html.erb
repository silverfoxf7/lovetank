<center><h1>Browse All Bids in PLE</h1></center>

  <table class="jobposts" summary="User jobposts">
    <%= render :partial => 'shared/bidfeed_item', :collection => @bidfeed_items %>
  </table>

  <%= will_paginate @bidfeed_items %>
